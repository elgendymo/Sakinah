# Vercel Project Settings
# This file helps Vercel understand your monorepo structure

# Root directory for the Next.js app
ROOT_DIRECTORY=apps/ui

# Build settings optimized for monorepo
BUILD_COMMAND=turbo run build --filter=@sakinah/web
INSTALL_COMMAND=npm install --workspace-root --include-workspace-root

# Memory and performance optimization
NODE_OPTIONS=--max_old_space_size=8192
NEXT_TELEMETRY_DISABLED=1

# Vercel Functions (if needed)
FUNCTIONS_DIRECTORY=apps/api/src
